import Link from "next/link";
import { BadgeCheck, Blocks, FileCheck2, GitBranch, Scale, ShieldCheck } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const features = [[GitBranch, "Release promises", "Teams publish specific claims about migrations, security fixes, API changes, and shipped features."], [FileCheck2, "Evidence trails", "GitHub releases, documentation, issue threads, packages, and CI pages become reviewable evidence."], [Scale, "Semantic challenges", "Users can challenge whether the shipped release materially fulfilled what was promised."], [Blocks, "Bonded accountability", "Teams lock GEN against a promise so fulfillment has consequences beyond a marketing statement."], [BadgeCheck, "Consensus verdicts", "GenLayer validators compare the promise with live release evidence and record the result on-chain."], [ShieldCheck, "Abstention by design", "Unreachable or contradictory sources produce an undetermined result instead of a forced judgment."]] as const;

export default function LandingPage() { return <div className="space-y-16"><section className="grid items-center gap-8 pt-8 lg:grid-cols-[1.05fr_0.95fr]"><div><p className="mb-4 inline-block rounded-full border border-amberline/70 px-4 py-1 text-xs uppercase tracking-[0.28em] text-amberline">RELEASE ACCOUNTABILITY · GENLAYER STUDIONET</p><h1 className="max-w-4xl text-4xl font-black leading-tight text-vault-100 md:text-6xl">Ship what you promised.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-vault-200">Changel lets open-source and SaaS teams bond a release promise, publish evidence, and let GenLayer judge whether the shipped release materially delivered on its claims.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/cases/new"><Button size="lg">Create a promise</Button></Link><Link href="/cases"><Button size="lg" variant="outline">Explore promises</Button></Link></div></div><Card><CardHeader><p className="font-mono text-xs uppercase tracking-[0.24em] text-vault-700">Live workflow</p><CardTitle>Promise → release → challenge → verdict</CardTitle></CardHeader><CardContent><div className="grid gap-3">{[["01", "Team defines a measurable release promise and locks a GEN bond."], ["02", "The release ships with public evidence links."], ["03", "A user challenges the promise if the release does not match."], ["04", "GenLayer reads the sources and settles the accountability result."]].map(([step, text]) => <div key={step} className="flex gap-3 rounded-md border border-vault-700/20 p-3"><span className="font-mono text-sm font-black text-vault-700">{step}</span><p className="text-sm text-vault-950/75">{text}</p></div>)}</div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3">{features.map(([Icon,title,text]) => <Card key={title}><CardHeader><Icon className="mb-2 h-7 w-7 text-vault-700" /><CardTitle>{title}</CardTitle></CardHeader><CardContent><p className="text-sm leading-6 text-vault-950/75">{text}</p></CardContent></Card>)}</section><section className="rounded-lg border border-vault-300/15 bg-vault-900/80 p-8"><div className="grid gap-4 lg:grid-cols-[1fr_auto] lg:items-center"><div><h2 className="text-2xl font-black">A changelog is a promise, not a verdict.</h2><p className="mt-3 max-w-3xl text-vault-200">Version numbers can prove that code shipped. They cannot prove that a migration is documented, a security claim is addressed, or a feature works as described. Changel puts that semantic question into GenLayer consensus.</p></div><Link href="/dashboard"><Button variant="secondary">Open release desk</Button></Link></div></section></div>; }
